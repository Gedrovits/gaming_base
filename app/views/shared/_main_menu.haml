%nav.navbar.navbar-fixed-top.navbar-default
  .container
    .navbar-header
      %button.navbar-toggle.collapsed{ 'data-toggle' => 'collapse', 'data-target' => '#top_menu' }
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{ href: root_path }
        = fa_icon('gamepad')
        Gaming Base
        %sup
          &alpha;
    -#&beta;
    #top_menu.collapse.navbar-collapse
      - if user_signed_in?
        = render 'shared/private_menu'
      - else
        = render 'shared/public_menu'

.nested-fields.col-md-3
  = f.input :language, collection: Settings.locales
  = f.input :native
  = f.input :understanding, collection: LanguageProficiency.understandings.keys.to_a, as: :radio_buttons
  = f.input :speaking, collection: LanguageProficiency.speakings.keys.to_a, as: :radio_buttons
  = f.input :writing, collection: LanguageProficiency.writings.keys.to_a, as: :radio_buttons
  = link_to_remove_association "remove language proficiency", f

.page-header
  %h1
    Users
    .pull-right
      = link_to 'New', new_user_path, class: 'btn btn-primary'
%table.table
  %thead
    %tr
      %th Email
      %th Active
      %th Is Core
      %th Locale
      %th TimeZone
  %tbody
    = render @users
